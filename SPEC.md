# UI 组件库 & 报告工具 · 设计规格文档

> 版本: v3.7 | 2026-05-26 | 配置驱动 + 分章生成 + 可视化选择 + 引导推荐 + 导入告警 + 报告自包含 + 批量章节导入 + 标签化受众 + 操作引导
> - v1.0（废弃）：iframe + report 提示词工具双系统
> - v2.0（废弃）：去掉 report，纯 Claude Code 对话，缺乏项目管理和分步控制
> - v3.7（当前）：精简项目结构，移除 hooks/assemble.cjs 等冗余文件

---

## 文档层级与引用导向

本项目有三份核心规则文档，层级关系如下：

```
SPEC.md（本文件）         ← 顶层：设计规格书，定义系统是什么、有哪些功能
  ↓ 指导
.claude/CLAUDE.md        ← 入口层：项目根指令，定义触发规则和顶层行为
  ↓ 委托
UI-lib/.claude.md        ← 执行层：组件库约束，定义具体生成规则和组件索引
```

**优先级（冲突时以上层为准）**：SPEC.md > .claude/CLAUDE.md > UI-lib/.claude.md

**各文档定位**：

| 文档 | 受众 | 用途 | 何时读 |
|------|------|------|--------|
| `SPEC.md` | 开发者、维护者 | 理解系统全貌、功能规格、变更历史 | 新加入项目、做架构决策 |
| `.claude/CLAUDE.md` | Claude Code | 触发规则、工作模式分发、对用户承诺 | Claude 每次对话自动加载 |
| `UI-lib/.claude.md` | Claude Code | 组件索引、生成规则、资源层级、禁止事项 | 生成报告/页面时按需读取 |

**典型阅读路径**：
- 新人入门 → 先读 SPEC.md 了解全貌，再按需查阅 CLAUDE.md
- 改功能 → 改代码 → 按文档同步规则更新 SPEC.md → 同步 .claude/CLAUDE.md 或 UI-lib/.claude.md
- 写规则 → 判断是顶层行为（放 .claude/CLAUDE.md）还是执行细节（放 UI-lib/.claude.md）

---

## 一、核心理念

**工具页面管配置，Claude Code 管生成，config.json 是桥梁。**

用户不再需要记住组件名、路径、主题名——在工具页面用表单勾选即可。配置保存为 config.json，Claude 读取后按章节结构化生成，每章独立文件，最终组装。

```
工具页面（纯配置读写）           config.json                   Claude Code
────────────────────          ──────────                   ──────────
新建项目 → 选目录 → 命名        版式/主题/模式                 读取 config.json
导入项目 → 选文件夹加载     ──→  章节定义 + 数据引用     ──→   分章生成 HTML
选版式/主题/组件                components 列表              逐章对话修改
定义章节 + 绑定数据                                        最终组装输出
保存配置（写入磁盘）
```

---

## 二、目录结构

```
AIProject/
├── .claude/
│   ├── CLAUDE.md                         # 项目根指令
│   ├── settings.local.json               # 权限配置
│   └── skills/                           # Claude Code 技能定义
│
├── UI-lib/                               # 组件库
│   ├── .claude.md                        # 组件库约束文档
│   ├── index.html                        # 组件预览页（开发用，人工浏览）
│   ├── manifest.json                     # 组件注册表（菜单树、路径、meta 引用）
│   ├── tokens/                           # 3 套 Design Token CSS
│   ├── components/                       # 7 个业务组件
│   ├── charts/                           # 2 个图表组件
│   ├── skeletons/                        # 3 个版式骨架
│   ├── templates/                        # 3 个完整案例
│   └── previews/                         # Token 色板预览
│
├── report-tool/                          # 报告工具
│   ├── index.html                        # 单页应用：纯配置读写工具
│   └── guide.html                        # 操作指引页
│
│   ※ 项目文件夹由用户自由选择位置（不限于 report-tool/ 内）
│       └── 货币基金分析报告/（示例，可在任意目录）
│           ├── config.json               # ★ 项目配置（Claude 指令）
│           ├── data/                     # 数据文件（任意格式）
│           │   ├── 产品数据.csv
│           │   ├── 收益归因分析.docx
│           │   └── 竞品对比.pdf
│           ├── chapter-01-cover.html     # 第1章：封面
│           ├── chapter-02-overview.html  # 第2章：产品表现概览
│           ├── chapter-03-attribution.html
│           ├── chapter-04-comparison.html
│           ├── chapter-05-team.html
│           └── output/
│               └── <projectName>.html     # 最终组装成品，以项目名称命名
│
├── SPEC.md                               # 本文件
└── landing.html                          # 首页
```

---

## 三、report-tool 工具页面

### 3.1 index.html — 单页应用（纯配置读写工具）

index.html 是一个纯粹的配置读写工具。左侧两个独立按钮（新建项目 / 导入项目），右侧为配置编辑区。

**位置策略**：新建时引导用户选择 `report-tool/projects/` 作为推荐路径（确保 Claude Code 能读取 UI-lib 和约束文档），但允许选择其他位置；导入时若项目不在 AIProject 目录树下则显示告警。

**初始状态（未加载项目时）**：右侧编辑区显示空状态占位——图标 + 引导文字"点击左侧「新建项目」开始创建，或「导入项目」加载已有配置"。版式/主题/章节编辑区全部隐藏，待项目加载后再显示。

```
┌──────────────────────────────────────────────────────────────┐
│  报告工具 · 配置编辑器                             │
├──────────────┬───────────────────────────────────────────────┤
│ 左侧面板     │ 右侧编辑区                                    │
│ (280px)     │                                              │
│             │  ┌──────────────────────────────────────┐   │
│ ┌──────────┐│  │ ⚠️ 该项目不在 AIProject 工作区内，    │   │
│ │ 新建项目  ││  │   Claude Code 可能无法读取组件库      │   │
│ └──────────┘│  │   （仅在导入外部项目时显示）           │   │
│ ┌──────────┐│  └──────────────────────────────────────┘   │
│ │ 导入项目  ││                                              │
│ └──────────┘│  ┌─ ① 基本设置 ──────────────────────────┐   │
│             │  │                                        │   │
│ ─────────── │  │ 版式（点击选择）：                       │   │
│ 项目信息     │  │ ┌──────────┐ ┌──────────┐ ┌──────────┐│   │
│ （加载后显示）│  │ │ ████████ │ │ ████████ │ │ ████████ ││   │
│             │  │ │ A4 横版  │ │Slide翻页 │ │ 自由滚动 ││   │
│ 📁 货币基金  │  │ │ 297×210  │ │ 全屏演示 │ │ 响应式   ││   │
│ 报告        │  │ │   ✓ 已选 │ │          │ │          ││   │
│ 路径:       │  │ └──────────┘ └──────────┘ └──────────┘│   │
│ .../proj... │  │                                        │   │
│ ✓ 工作区内  │  │ 主题（点击选择）：                       │   │
│             │  │ ┌──────────┐ ┌──────────┐ ┌──────────┐│   │
│ 状态: 3/5章 │  │ │ ████████ │ │ ████████ │ │ ████████ ││   │
│ [保存配置]  │  │ │ 商务蓝白 │ │ RC 浅蓝  │ │ 暗黑科技 ││   │
│             │  │ │   ✓ 已选 │ │          │ │          ││   │
│             │  │ └──────────┘ └──────────┘ └──────────┘│   │
│             │  │                                        │   │
│             │  │ 生成模式：                              │   │
│             │  │ ○ 分章生成（推荐，长报告逐章处理）       │   │
│             │  │ ○ 一次性生成（适合3-5页短报告）         │   │
│             │  │                                        │   │
│             │  │ 报告标签（可多选）：                     │   │
│             │  │ [高管汇报✓] [客户展示] [正式交付✓]    │   │
│             │  │ [内部协同] [培训材料] [方案建议]       │   │
│             │  └────────────────────────────────────────┘   │
│             │                                              │
│             │  ┌─ ② 章节定义 ───────────────────────┐   │
│             │  │  [批量输入] [逐个添加]  ← 标签页切换  │   │
│             │  │                                        │   │
│             │  │ ▸ 第1章 · 封面            status: ✓完成 │   │
│             │  │   标题 [封面_________________________] │   │
│             │  │   描述 [报告封面，含标题、日期、团队_] │   │
│             │  │                                        │   │
│             │  │   ┌─ 组件选择 ─────────────────────┐   │   │
│             │  │   │ 来自组件库（点击缩略图勾选）：    │   │   │
│             │  │   │ ┌──────┐ ┌──────┐ ┌──────┐    │   │   │
│             │  │   │ │ 缩略 │ │ 缩略 │ │ 缩略 │    │   │   │
│             │  │   │ │ 图A  │ │ 图B  │ │ 图C  │    │   │   │
│             │  │   │ │hero- │ │stat- │ │badge │    │   │   │
│             │  │   │ │card ✓│ │card  │ │-tag  │    │   │   │
│             │  │   │ └──────┘ └──────┘ └──────┘    │   │   │
│             │  │   │                                │   │   │
│             │  │   │ 自定义展示需求（可选）：         │   │   │
│             │  │   │ [如：左右两栏布局，左边3个数_]  │   │   │
│             │  │   └────────────────────────────────┘   │   │
│             │  │                                        │   │
│             │  │   数据文件 [data/产品数据.csv_] [选择] │   │
│             │  │  ──────────────────────────────────    │   │
│             │  │                                        │   │
│             │  │ ▸ 第2章 · 产品表现概览  status: ●草稿 │   │
│             │  │   ...（折叠）                          │   │
│             │  │                                        │   │
│             │  │ ▸ 第3章 · 收益归因分析  status: ○待生成│   │
│             │  │   ...（折叠）                          │   │
│             │  └────────────────────────────────────────┘   │
│             │                                              │
│             │  ┌─ ③ 下一步 ─────────────────────────────┐   │
│             │  │                                        │   │
│             │  │  ① 确认配置已保存                       │   │
│             │  │     检查章节进度，必要时手动保存         │   │
│             │  │                                        │   │
│             │  │  ② 切换到 Claude Code                   │   │
│             │  │     终端进入 AIProject 目录启动对话      │   │
│             │  │                                        │   │
│             │  │  ③ 说「开始生成报告：项目名」           │   │
│             │  │     Claude 自动读取配置逐章生成          │   │
│             │  │                                        │   │
│             │  │  ⏱ 每5分钟自动保存一次                  │   │
│             │  └────────────────────────────────────────┘   │
└──────────────┴──────────────────────────────────────────────┘
```

### 3.2 功能清单

| 区域 | 功能 | 说明 |
|------|------|------|
| 左侧面板 | 新建项目 | **独立按钮**。点击 → 弹窗输入项目名 → 确认 → 系统文件对话框选择保存目录（界面提示推荐 `report-tool/projects/`）→ 在该目录下创建 `项目名/` 文件夹 + 初始 config.json → 加载到编辑器 |
| 左侧面板 | 导入项目 | **独立按钮**。点击 → 系统文件对话框选择已有项目文件夹 → 读取 config.json → 加载到编辑器。若项目路径不在 AIProject 目录树下，顶部显示黄色告警条 |
| 左侧面板 | 项目信息 | 加载项目后显示：项目名、磁盘路径、是否在 AIProject 工作区内（✓/⚠️）、章节进度。提供 [保存配置] 按钮手动写回磁盘 |
| ① 基本设置 | 版式选择 | **平铺卡片单选**，每张卡显示缩略示意图 + 名称 + 尺寸说明，点击选中高亮边框 |
| ① 基本设置 | 主题选择 | **平铺卡片单选**，每张卡显示配色色条预览 + 名称，点击选中高亮边框 |
| ① 基本设置 | 报告标签 | **标签芯片多选**，6 个预设标签（高管汇报/客户展示/内部协同/正式交付/培训材料/方案建议），点击切换选中。选中的标签帮助 AI 调整报告语气、深度和排版风格 |
| ① 基本设置 | 生成模式 | 单选按钮：分章 / 一次性 |
| ② 章节定义 | 章节管理 | 两种模式（标签页切换）：**批量输入**（默认，左侧）— 自由粘贴大纲，AI 智能理解；**逐个添加**（右侧）— 逐章填写标题/描述/组件/数据 |
| ② 章节定义 | 组件选择 | **缩略图网格**，从 manifest.json 加载组件列表，用 CSS 线框图展示缩略图。点击勾选/取消。支持多选 |
| ② 章节定义 | 自定义组件 | 文本区，用户自由描述不在组件库中的展示需求（如"左右两栏，左边放3个核心数字，右边放趋势图"），写入 config 的 `customLayout` 字段 |
| ② 章节定义 | 数据绑定 | 选择项目 data/ 下的文件，或留空手动输入 |
| ② 章节定义 | 标题/描述 | 文本输入，`desc` 作为 Claude 的生成指令 |
| ③ 下一步 | 操作引导 | 3 步引导卡片：① 确认配置已保存 → ② 切换到 Claude Code → ③ 说「开始生成报告」。替代原来的操作按钮区，提供清晰的操作指引 |
| ③ 下一步 | 自动保存 | 每 5 分钟自动将当前配置写入项目目录的 config.json。引导卡片中显示上次保存时间。用户也可点击左侧 [保存配置] 按钮手动立即保存 |
| - | 初始状态 | 未加载项目时，右侧显示空状态引导："点击左侧「新建项目」开始创建，或「导入项目」加载已有配置"。编辑区内容全部隐藏 |

### 3.3 设计细节

**左侧按钮** — 两个等宽的独立按钮，垂直排列：

- **"新建项目"**：主色调按钮，图标 + 文字。点击流程：
  1. 弹窗输入项目名称
  2. 点击"选择保存位置"，调用 `showDirectoryPicker()`，弹窗旁显示提示文字 **"💡 推荐保存到 report-tool/projects/ 下，确保 Claude Code 能读取组件库"**
  3. 在所选目录下创建 `项目名/` 文件夹，写入初始 config.json
  4. 加载到编辑器，左侧显示项目信息

- **"导入项目"**：次要按钮，图标 + 文字。点击流程：
  1. 调用 `showDirectoryPicker()` 让用户选择已有项目文件夹
  2. 检查文件夹内是否有 config.json，无则弹错误提示
  3. 读取并解析，加载到编辑器
  4. **位置检测**：判断项目路径是否在 AIProject 目录树下（即路径是否包含 `AIProject/`）。若否，右侧编辑区顶部显示黄色告警条：<br>`⚠️ 该项目不在 AIProject 工作区内，Claude Code 可能无法读取 UI-lib 组件库和约束文档`

两个按钮下方显示当前项目信息（仅在加载项目后可见）：项目名、磁盘完整路径、工作区状态（✓ 在工作区内 / ⚠️ 不在工作区内）、章节进度条、手动 [保存配置] 按钮。

**版式/主题卡片** — 用 CSS 绘制简单的视觉示意（版式卡片画 mini 页面轮廓，主题卡片画 5-6 个色块代表配色），不依赖外部图片。选中态用 2px 主题色边框 + 浅色背景。

**组件缩略图** — 两种实现方案：
- 方案A（轻量）：用 CSS 画组件的极简线框图示意（如 stat-card 画一个矩形+大数字+标题线）
- 方案B（准确）：用隐藏 iframe 加载组件 HTML，截图或用 `transform: scale()` 缩略展示

推荐方案A，无需网络请求，加载快，足够区分不同组件。缩略图下方标注组件名和简短描述。

**自定义展示需求** — 每个章节的组件选择区下方有一个文本输入框，placeholder 如"不选组件库的组件时，在此描述你想要的展示方式..."。写入 config.json 时存入 `chapters[].customLayout` 字段。Claude 读取时，如果 `customLayout` 非空，则优先按自定义描述生成，忽略 `components` 列表；如果 `customLayout` 为空，则按 `components` 列表组装。

### 3.4 技术栈

```
Vue 3              → unpkg CDN
Tailwind CSS       → cdn.tailwindcss.com
纯前端 SPA          → 单页面：index.html（配置编辑器）
文件系统访问         → File System Access API（showDirectoryPicker 选择目录读写）
位置检测            → 判断项目路径是否包含 "AIProject/"，决定是否显示告警条
自动保存            → 每 5 分钟定时写入 config.json + 手动保存按钮
数据存储            → config.json（读写磁盘）
组件列表            → fetch ../UI-lib/manifest.json
```

**浏览器兼容性**：File System Access API 目前仅 Chrome/Edge/Opera 完整支持。Safari 和 Firefox 不支持 `showDirectoryPicker()`。工具页面打开时检测 API 可用性，若不支持则在顶部显示红色提示条："⚠️ 当前浏览器不支持文件系统访问，请使用 Chrome 或 Edge 浏览器打开此工具"。

---

## 四、config.json 规范

### 4.1 完整结构

```json
{
  "projectName": "货币基金分析报告",
  "createdAt": "2026-05-25T10:00:00Z",
  "updatedAt": "2026-05-25T14:30:00Z",
  "layout": "a4-landscape",
  "theme": "business-bluewhite",
  "mode": "chapter",
  "tags": ["高管汇报", "正式交付"],
  "chapterOutline": "封面：展示标题、日期、审计主体\n核心财务指标审计：资产负债率、流动比率趋势分析\n贷款用途与资金流向核对：逐笔核对合规性",
  "chapters": [
    {
      "id": "cover",
      "order": 1,
      "title": "封面",
      "desc": "报告封面页，包含：标题'货币基金月度收益归因分析服务方案'、副标题简述服务目标、日期2026年5月、容诚团队名称",
      "components": ["hero-card"],
      "customLayout": null,
      "dataFile": null,
      "status": "done"
    },
    {
      "id": "overview",
      "order": 2,
      "title": "产品表现概览",
      "desc": "展示核心KPI指标：七日年化收益率2.15%（↑）、万份收益0.5892（→）、产品规模156.8亿（↑）。用数据卡片网格布局，突出趋势方向。",
      "components": ["stat-card", "badge-tag"],
      "dataFile": "data/产品数据.csv",
      "status": "draft"
    },
    {
      "id": "attribution",
      "order": 3,
      "title": "收益归因分析",
      "desc": "五维度收益拆解：票息收益、杠杆套息、久期管理、交易骑乘、费用影响。用流程步骤展示归因方法论，数据表格展示各维度贡献度，柱状图对比各月表现。",
      "components": ["process-step", "data-table", "bar-chart"],
      "customLayout": null,
      "dataFile": "data/收益归因分析.docx",
      "status": "pending"
    }
  ]
}
```

### 4.2 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `projectName` | string | 项目名称，即子文件夹名 |
| `layout` | enum | 版式：`a4-landscape` / `slide-deck` / `scroll-page` |
| `theme` | enum | 主题：`business-bluewhite` / `rc-lightblue` / `dark-tech` |
| `mode` | enum | `chapter` = 分章生成，`full` = 一次性生成 |
| `tags` | string[] | 报告标签（可多选）。可选值：`高管汇报` / `客户展示` / `内部协同` / `正式交付` / `培训材料` / `方案建议`。帮助 AI 调整报告语气、深度和排版风格。可为空数组 |
| `chapterOutline` | string | 用户通过批量输入粘贴的原始章节大纲。格式不限（markdown/纯文本/对话记录），Claude 用 AI 智能理解并列出章节结构供用户确认。不为空且 chapters 为空时优先触发大纲理解流程 |
| `chapters[].id` | string | 章节唯一标识，用于文件名 |
| `chapters[].title` | string | 章节中文标题 |
| `chapters[].desc` | string | **关键字段**：章节内容描述，直接作为 Claude 的生成指令。用户写什么，Claude 就生成什么 |
| `chapters[].components` | string[] | 该章使用的组件列表，从 manifest.json 的可选组件中勾选。如果 `customLayout` 非空，此字段可能为空数组 |
| `chapters[].customLayout` | string | null 或自定义展示需求描述。用户不选组件库组件时，在此自由描述想要的布局和效果。非空时 Claude 优先按此描述生成，忽略 components |
| `chapters[].dataFile` | string | null 或 data/ 下的文件路径。格式不限（csv/xlsx/docx/pdf/txt），Claude 自行读取 |
| `chapters[].status` | enum | `pending` → `draft` → `done`，工具页面和 Claude 都可更新 |

### 4.3 config.json 的双重身份

1. **工具的存档** — 编辑器读写，保存用户配置，关闭后重新打开不丢失
2. **Claude 的指令** — Claude 读取后，清楚知道每章要写什么、用什么组件、数据在哪

---

## 五、Claude Code 工作流程

### 5.1 触发规则（需更新 .claude/CLAUDE.md）

```
当用户提到以下任一情况时，必须首先读取对应项目的 config.json：
- "继续生成报告" / "开始生成第X章" / "生成下一章"
- 指定了某个报告项目名称（如"货币基金分析报告"）
- "组装报告" / "合并章节" / "输出完整报告"

**查找 config.json 的方式**：
1. 优先扫描 `report-tool/projects/` 下匹配项目名的子目录（推荐路径，多数项目在此）
2. 若未找到，询问用户提供项目文件夹的路径
3. 用户也可在对话中直接给出路径（如"打开 /Users/xxx/我的项目/"）

读取 config.json 后，按以下流程执行。
```

### 5.2 分章生成流程（mode: chapter）

```
Claude 读取 config.json
  → 找到第一个 status = "pending" 的章节
    → 读 UI-lib manifest.json 了解组件路径
    → 读骨架 HTML（根据 layout）
    → 读主题 CSS（根据 theme）
    → 读该章指定的 components 的 meta.json + HTML
    → 如果有 dataFile，读 data/ 下的数据文件
    → 根据章节 desc 生成 chapter-XX-xxx.html
    → 更新 config.json 中该章 status 为 "draft"
    → 告知用户：第X章已生成，可预览，输入"继续"生成下一章
      ↓
用户对话修改当前章节（改内容、调样式）
  → 确认无误后，用户说"继续"或"下一章"
    → Claude 更新当前章 status 为 "done"
    → 继续处理下一个 pending 章节
```

### 5.3 一次性生成流程（mode: full）

```
Claude 读取 config.json
  → 遍历所有章节，读取所有引用的组件和数据
  → 分批生成（每轮 ≤ 800 行）
    → 第1轮：封面 + 前2-3章
    → 第2轮：后续章节
    → 第3轮：收尾 + 页码 + 打印控制
  → 输出完整 HTML
```

### 5.4 组装流程

```
用户："组装报告"

Claude 读取所有 status=done 的章节
  → 按 order 合并所有 chapter HTML
  → 提取每个 chapter 的 <body> 内容
  → 统一内联主题 CSS、页面编号
  → 输出 <项目路径>/output/<projectName>.html（以 config.json 中的 projectName 命名）
```

### 5.5 数据文件处理

`dataFile` 字段存相对路径（相对于项目子文件夹），Claude 按路径读取。各格式处理方式详见 `UI-lib/.claude.md` 3.7 节。

---

## 六、UI-lib 组件库（保持不变）

UI-lib 的结构和规范保持 v2.0 的设计不变。详见 UI-lib/.claude.md。

唯一新增：index.html 通过 `fetch('../UI-lib/manifest.json')` 读取组件列表，渲染为可勾选的缩略图卡片。

---

## 七、关键设计决策

### 7.1 项目位置策略（方案A：引导推荐 + 导入告警）

**新建项目 → 引导推荐路径；导入项目 → 检测位置并告警。**

- **新建时**：用户可自由选择电脑任意目录保存项目，但界面上明确提示推荐 `report-tool/projects/`，引导大多数用户选对位置
- **导入时**：不限制用户选择的文件夹位置，但加载后自动检测项目路径是否在 AIProject 目录树下。若不在，右侧编辑区顶部显示黄色告警条，提醒用户 Claude Code 可能无法读取 UI-lib 组件库和约束文档

这样既保持了工具的纯粹性（不强制绑定固定目录），又通过 UI 引导和告警降低了用户把项目放到错误位置的概率。

**检测逻辑**：判断项目文件夹的绝对路径是否包含 `AIProject/`（或 `AIProject` 目录名），简单可靠，无需后端服务。

### 7.2 config.json 而非 localStorage

用文件而不是浏览器存储：
- config.json 是真实文件，Claude 可以直接读写
- 跨会话、跨设备（git 同步）不丢失
- localStorage 只能存字符串，且 Claude 无法直接访问

### 7.3 章节独立文件

每章一个 HTML 文件的好处：
- 单文件体积小（200-400行），Claude 读写高效
- 修改某一章不影响其他章
- 多轮对话不会超出上下文限制
- status 字段追踪进度，随时知道完成情况

### 7.4 报告文件自包含

生成的报告 HTML 必须是自包含的最终交付物。主题 CSS token 必须内联到 `<style>` 标签中，禁止使用相对路径（如 `../../UI-lib/`）引用组件库资源。这样报告文件可以：
- 脱离项目目录结构，复制到任意位置直接打开
- 在 Claude Code 客户端预览时正确显示颜色
- 发送给他人时无需附带整个项目文件夹

CDN 绝对 URL 引用（Tailwind、Vue、ECharts）不受此限制。

### 7.5 A4 页面内容不可溢出

A4 横版骨架的 `.a4-page` 显式设置 `box-sizing: border-box`。打印时固定 297mm×210mm 且 `overflow: hidden`；屏幕浏览时通过 `width: min(297mm, calc(100vw - 32px))` + `aspect-ratio: 297/210` 自适应视口宽度、保持 A4 长宽比。生成的内容必须在可视区域内最佳排版，不可溢出被裁剪。

- **可用内容区**：扣除 padding 后约 253mm × 174mm
- **字号约束**：标题 28-36px，正文 14-15px，辅助 11-13px，数据大字 40-48px
- **列数约束**：详情卡片 ≤ 3 列，迷你统计 ≤ 4 列
- **表格约束**：单页 ≤ 12-15 行，超出分页
- **图表约束**：高度 ≤ 260-280px
- **溢出处理**：内容超出一页时必须拆分新的 `.a4-page` section，禁止在一个 section 内堆砌

### 7.6 两层确认，避免跑偏

```
第1层（工具页面）：用户选版式/主题/组件/章节 → 保存 config.json
第2层（Claude 对话）：首轮只出框架 → 确认结构 → 逐步填充内容
```

工具页面保证"方向对"，Claude 对话保证"内容对"。

---

## 八、与 v2.0 的变更对比

| | v2.0 | v3.0 | v3.3（当前） |
|---|---|---|---|
| 项目管理 | 无，每次对话从零开始 | report-tool/projects/ 目录，持久化 | 任意磁盘位置，新建/导入，推荐路径引导 |
| 配置方式 | 用户口述需求，Claude 猜测 | index.html 表单（平铺卡片+缩略图），config.json 精确指令 | 同 v3.0 |
| 项目位置 | N/A | 限制在 report-tool/projects/ | 引导推荐 projects/，允许任意位置，导入时检测告警 |
| 工具定位 | 无工具 | 项目管理 + 配置编辑 | 纯配置读写工具 |
| 生成控制 | 全靠对话引导 | 分章模式，每章独立，进度可追踪 | 同 v3.0 |
| 数据引用 | 用户口述或用提示词描述 | data/ 文件夹，config 引用路径 | 同 v3.0 |
| 组装方式 | Claude 一次性生成 | 分章生成 + Claude 合并输出 | Claude 读取 done 章节合并 |
| 跨会话支持 | 依赖对话历史，离开会话丢失 | config.json 持久化，下次对话直接继续 | 同 v3.0 |
| 自动保存 | 无 | 手动保存 | 每 5 分钟自动保存 + 手动保存 |
| UI-lib | 不变 | 不变 | 不变 |

---

## 九、实施计划

| 阶段 | 内容 | 说明 |
|------|------|------|
| **P1** | 创建 `report-tool/` 目录 | 基础结构 |
| **P2** | 编写 `report-tool/index.html` | 单页应用：左侧新建/导入按钮 + 右侧配置编辑器 + File System Access API |
| **P3** | 实现版式/主题平铺卡片选择 | CSS 绘制缩略示意图 + 单选高亮 |
| **P4** | 实现组件缩略图选择 + 自定义描述 | CSS 线框图缩略 + 文本输入框，customLayout 写入 config |
| **P5** | 更新 `.claude/CLAUDE.md` | 新增触发规则：发现 config.json → 按配置生成 |
| **P6** | 更新 `UI-lib/.claude.md` | 新增分章生成流程、config.json 驱动规则、customLayout 优先规则 |

---

## 十、变更记录

| 日期 | 变更 |
|------|------|
| 2026-05-22 | v1.0 初始版本，iframe + report 双系统方案 |
| 2026-05-22 | v2.0 去掉 report 工具，改为 Claude Code 原生方案 |
| 2026-05-25 | v3.0 新增 report-tool 配置驱动 + 分章生成 + 项目管理 |
| 2026-05-25 | v3.1 合并 index.html/editor.html 为单页；版式/主题改为平铺卡片；组件选择改为缩略图+自定义描述；去掉复制提示词按钮 |
| 2026-05-25 | v3.2 左侧改为"新建项目"/"导入项目"两个独立按钮；项目位置自由化，不再限制于 report-tool/projects/；工具定位变为纯配置读写工具 |
| 2026-05-25 | v3.3 采用方案A：新建时引导推荐路径（report-tool/projects/），导入时检测路径是否在 AIProject 内并显示告警条；新增初始空状态、每5分钟自动保存、浏览器兼容性检测；更新 CLAUDE.md 和 UI-lib/.claude.md 中项目查找逻辑（优先扫描 projects/，未找到询问路径）；assemble.cjs 改为接受绝对路径 |
| 2026-05-25 | v3.4 修复报告文件自包含问题：主题 CSS token 改为内联到 `<style>` 标签，禁止相对路径引用 UI-lib 资源，确保报告脱离项目目录也可正常打开 |
| 2026-05-25 | v3.5 章节定义新增「批量输入」模式：自由粘贴大纲（格式不限）→ 保存到 config.chapterOutline → Claude 用 AI 智能理解章节结构（非正则解析）→ 列出供用户确认后再生成。config.json 新增 chapterOutline 字段，UI-lib/.claude.md 新增 3.2 大纲理解流程 |
| 2026-05-25 | v3.6 UI 重组： 基本设置新增「报告标签」多选芯片（6 个预设标签），替代原来的受众/用途下拉框，config.json 新增 tags 字段； 章节定义批量输入/逐个添加标签页左右互换（批量在左为默认）； 第三节从「操作」改为「下一步」，去掉组装/预览按钮，改为 3 步操作引导卡片 |
| 2026-05-26 | v3.7 精简项目结构：移除 hooks/ 对话保存系统、assemble.cjs 组装脚本、timeline-chart 图表组件、config.cjs/my-design.md/chat_history 等冗余文件；新增 .claude/skills/ 技能定义目录和 report-tool/guide.html 操作指引页 |

---

## 十一、用户操作指引

> 以下是从零开始，使用本工具完成一份报告的完整操作流程。

---

### 11.0 准备工作

开始之前，确保你具备以下条件：

- **AIProject 项目**已在本地克隆或下载，知道其根目录路径
- **Chrome 或 Edge 浏览器**（Safari/Firefox 暂不支持 File System Access API，无法使用报告工具）
- **Claude Code** 已安装并配置，工作目录设在 AIProject 根目录下
- （可选）提前准备好报告需要的数据文件（csv、docx、pdf 等），放到方便引用的位置

---

### 11.1 第一步：浏览组件库（可选，约 5 分钟）

在开始配置项目前，建议先了解有哪些可用的版式、主题和组件，心中有数。

1. 用浏览器打开 `UI-lib/index.html`
2. 页面展示所有可用资源：

   | 资源类型 | 内容 | 用途 |
   |---------|------|------|
   | 版式骨架 | A4 横版 / PPT 翻页 / 自由滚动 | 决定报告的页面结构 |
   | 主题 | 商务蓝白 / RC 浅蓝 / 暗黑科技 | 决定报告的配色方案 |
   | 组件 | 数据卡片、时间线、流程步骤、圆形编号、标签徽章、数据表格、封面卡片 | 章节中的内容模块 |
   | 图表 | 柱状图、折线图 | 可视化数据展示 |
   | 模板 | 4 个完整案例 | 参考成品效果 |

3. 记下感兴趣的版式、主题和组件名，后续配置时可直接勾选

> 如果你已经熟悉组件库，可以跳过此步直接进入配置。

---

### 11.2 第二步：打开报告工具

用 Chrome 或 Edge 打开 `report-tool/index.html`。

**首次打开时**，页面左侧显示两个按钮，右侧为空状态引导文字：

> "点击左侧「新建项目」开始创建，或「导入项目」加载已有配置"

如果浏览器不支持 File System Access API（如 Safari），顶部会显示红色提示条，需换用 Chrome 或 Edge。

---

### 11.3 第三步：新建项目

1. 点击左侧 **「新建项目」** 按钮
2. 弹出对话框，输入项目名称（如"货币基金分析报告"），点击确认
3. 系统弹出文件夹选择对话框，界面会提示：

   > 💡 推荐保存到 `report-tool/projects/` 下，确保 Claude Code 能读取组件库

4. **建议选择** `report-tool/projects/` 目录，这样 Claude Code 能自动找到你的项目。当然也可以选择其他位置
5. 确认后，工具会在所选目录下创建 `项目名/` 文件夹，并自动生成初始 `config.json`
6. 左侧面板显示项目信息：项目名、磁盘路径、工作区状态（✓ 在工作区内）、章节进度

**此时项目文件夹结构：**
```
report-tool/projects/货币基金分析报告/
├── config.json          ← 刚生成的初始配置
└── data/                ← 稍后手动创建，放入数据文件
```

---

### 11.4 第四步：配置项目

项目加载后，右侧编辑区显示三段配置区域。

#### ① 基本设置

**版式选择**（三选一）：
- **A4 横版**（297×210）— 适合打印和 PDF 导出的正式报告
- **PPT 翻页**（全屏演示）— 适合 PPT 风格的演示文稿
- **自由滚动**（响应式）— 适合网页浏览的长页面

点击卡片选中，选中态有蓝色边框高亮。

**主题选择**（三选一）：
- **商务蓝白** — 稳重、专业，适合企业正式报告
- **RC 浅蓝** — 清爽、现代，适合演示文稿
- **暗黑科技** — 科技感，适合数据产品展示

点击卡片选中，选中态有蓝色边框高亮。

**生成模式**（二选一）：
- **分章生成**（推荐）— 每章独立生成和修改，适合 3 章以上的长报告
- **一次性生成** — 一次输出完整报告，适合 3-5 页短报告

**报告标签**（可多选）：
以标签芯片形式平铺展示，点击切换选中/取消。6 个预设标签：

| 标签 | 含义 | AI 调整方向 |
|------|------|------------|
| 高管汇报 | 面向高层管理者 | 结论导向、战略视角、精简有力 |
| 客户展示 | 面向外部客户 | 价值传递、信任建立、专业亲和 |
| 内部协同 | 团队内部使用 | 细节完整、可操作性、数据详尽 |
| 正式交付 | 正式交付物 | 严谨规范、格式标准、可归档 |
| 培训材料 | 培训教学用途 | 循序渐进、图文并茂、易于理解 |
| 方案建议 | 竞标或提案 | 突出优势、说服力强、对比清晰 |

选中的标签会帮助 AI 调整报告的语气、深度和排版风格。可不选，AI 会根据项目名称自行判断。

#### ② 章节定义

这是配置的核心。支持两种添加方式：

**方式一：批量输入**（推荐，默认显示在左侧，适合已有明确大纲）

切换到「批量输入」标签页，将提前准备好的章节大纲自由粘贴（格式不限），切换到 Claude Code 对话时，AI 会自动：

1. 理解你的大纲结构（无论 markdown、纯文本、编号列表还是口语化描述）
2. 列出识别出的章节供你确认
3. 确认后写入 config.json，按正常流程逐章生成

无需纠结格式——越详细效果越好，但怎么写都行。AI 负责理解意图，不是正则解析。

**方式二：逐个添加**（适合边想边配）

点击 **「添加章节」** 逐章填写标题、描述、组件和数据。适合还没有明确大纲、需要边配置边思考的场景。

每个章节包含：

| 字段 | 说明 | 示例 |
|------|------|------|
| 标题 | 章节名称 | "封面"、"产品表现概览"、"收益归因分析" |
| 描述 | **关键字段**，直接告诉 Claude 要生成什么内容 | "展示核心 KPI 指标：七日年化收益率 2.15%（↑）、万份收益 0.5892（→）、产品规模 156.8 亿（↑）。用数据卡片网格布局" |
| 组件 | 从组件库勾选该章使用的组件（多选） | hero-card、stat-card、data-table |
| 自定义布局 | 不选组件时，自由描述想要的布局方式 | "左右两栏，左边放 3 个核心数字，右边放趋势图" |
| 数据文件 | 绑定 `data/` 下的文件作为数据来源 | data/产品数据.csv |

**章节的三种状态：**
- ○ 待生成（pending）— 尚未开始生成
- ● 草稿（draft）— 已生成，待确认修改
- ✓ 完成（done）— 已确认，可以组装

**组件选择 vs 自定义布局：**
- 如果勾选了组件库中的组件，Claude 会按组件模板生成
- 如果填写了 `自定义布局`，Claude 会优先按你的描述生成，忽略组件列表
- 两者也可以同时使用：自定义布局为主，组件列表作为补充参考

**关于数据文件：**
- 在项目文件夹下创建 `data/` 子目录
- 将 csv、docx、pdf 等数据文件放入其中
- 在章节配置中点「选择」绑定对应的数据文件
- Claude 生成报告时会自动读取数据文件内容

#### ③ 下一步

页面底部不再显示操作按钮，改为 **3 步操作引导卡片**：

1. **确认配置已保存** — 检查左侧项目信息确认章节进度，必要时点击「保存配置」。显示上次自动保存时间
2. **切换到 Claude Code** — 在终端中进入 AIProject 目录，启动 Claude Code 对话
3. **说「开始生成报告：项目名称」** — Claude 会自动读取 config.json，智能理解大纲（如有），逐章生成并等你确认。全部完成后说「组装报告」输出完整文件

引导卡片下方附有简短说明和自动保存提示。

---

### 11.5 第五步：导入已有项目

如果你之前创建过项目，或在其他电脑上配置了项目：

1. 点击左侧 **「导入项目」** 按钮
2. 系统弹出文件夹选择对话框，选择已有的项目文件夹（包含 `config.json` 的那个目录）
3. 工具读取 `config.json` 并加载到编辑器
4. 如果所选文件夹**不在 AIProject 目录树下**，右侧顶部会显示黄色告警条：

   > ⚠️ 该项目不在 AIProject 工作区内，Claude Code 可能无法读取 UI-lib 组件库和约束文档

5. 此时建议用"新建项目"重新在 `report-tool/projects/` 下创建，或手动将项目文件夹移入 AIProject 目录树下

---

### 11.6 第六步：保存配置

配置有**两种保存方式**：

| 方式 | 触发 | 说明 |
|------|------|------|
| 自动保存 | 每 5 分钟自动触发 | 将当前所有配置写入项目目录的 `config.json`，页面底部小字显示上次保存时间 |
| 手动保存 | 点击左侧 [保存配置] 按钮 | 立即写入磁盘，适合在关闭页面前手动确认保存 |

配置保存后，关闭浏览器也不会丢失。下次用"导入项目"打开即可继续。

---

### 11.7 第七步：切换到 Claude Code 生成报告

配置满意后，开始生成报告。

1. **确保 Claude Code 的工作目录是 AIProject 根目录**
2. 在终端启动 Claude Code 对话
3. 说 **"开始生成报告"** 或直接提项目名（如"生成货币基金分析报告"）

Claude 会自动：
- 扫描 `report-tool/projects/` 找到匹配的项目
- 读取项目的 `config.json`
- 读取 UI-lib 的 manifest.json、骨架、主题、组件
- 找到第一个 status 为 `pending` 的章节开始生成

**如果 Claude 找不到项目：**
- 告诉 Claude 项目的绝对路径（如"项目在 /Users/xxx/report-tool/projects/货币基金分析报告/"）
- Claude 会直接读取该路径下的 config.json

---

### 11.8 第八步：逐章确认与修改

**生成第一轮（Claude 自动）：**

```
Claude 读取 config.json
  → 找到第一个 pending 章节
  → 读取该章所需的组件和数据
  → 生成 chapter-XX-xxx.html
  → 更新 config.json 中该章 status 为 "draft"
  → 告知用户："第1章·封面已生成，可预览，确认后说'继续'生成下一章"
```

**用户确认修改（对话交互）：**

此时你可以：
- 查看生成的 HTML 文件内容
- 对 Claude 提出修改要求，如：
  - "标题字号再大一点"
  - "把数据换成最新的"
  - "配色改成和主题一致"
  - "第三段放到第二段前面"
- 修改满意后说 **"继续"** 或 **"下一章"** 或 **"生成第 X 章"**
- Claude 将当前章 status 更新为 `done`，然后处理下一章

**进度追踪：**
- 随时可以回到 `report-tool/index.html` 查看各章状态
- config.json 会实时反映每章的 status 变化
- 所有章节草稿文件保存在项目文件夹下

---

### 11.9 第九步：组装最终报告

所有章节 status 为 `done` 后，可以组装完整报告。

**在 Claude Code 中说"组装报告"**：
1. Claude 会读取所有 done 章节
2. 按 order 顺序合并所有 chapter HTML
3. 统一内联主题 CSS、页面编号
4. 输出到 `项目文件夹/output/<projectName>.html`（以项目名称命名）

**组装后的完整报告**包含：
- 统一的 CSS 样式和主题变量
- 连续的页面编号
- 打印控制的 @page/@media print 规则
- 所有章节按 order 顺序排列

---

### 11.10 流程总览图

```
┌──────────────────────────────────────────────────────────────────┐
│                        用户操作全流程                              │
├────────────┬──────────────┬───────────────┬──────────────────────┤
│  浏览阶段   │   配置阶段    │   生成阶段    │      输出阶段         │
│  (5 分钟)  │  (10 分钟)   │ (逐章交互)    │     (1 分钟)         │
├────────────┼──────────────┼───────────────┼──────────────────────┤
│            │              │               │                      │
│ 打开       │ 打开         │ 切到          │ 所有章节 done 后      │
│ UI-lib/   │ report-tool/ │ Claude Code   │                      │
│ index.html│ index.html   │               │                      │
│            │              │               │                      │
│ 浏览       │ 新建/导入    │ 说"开始       │ 说"组装报告"          │
│ 骨架       │ 项目         │ 生成报告"     │ 输出最终 HTML        │
│ 主题       │              │               │                      │
│ 组件       │ 配置         │ Claude        │ 输出                 │
│ 模板       │ 版式/主题    │ 逐章生成       │ output/              │
│            │ 章节/组件    │               │ <projectName>.html   │
│ 了解       │ 数据绑定     │ 用户确认      │                      │
│ 可用资源    │              │ 逐章修改       │ 完成！               │
│            │ 自动保存     │ 直到全部      │                      │
│            │ config.json  │ done          │                      │
│            │              │               │                      │
└────────────┴──────────────┴───────────────┴──────────────────────┘
```

---

### 11.11 常见问题

**Q: 必须先把数据文件放到 data/ 下吗？**
A: 建议这样做。配置时通过"数据绑定"选择文件，Claude 生成时会自动读取。也可以生成后再让 Claude 补充数据。

**Q: 可以在配置中途关闭页面吗？**
A: 可以。每 5 分钟自动保存 + 手动保存按钮，配置存在 config.json 中，下次"导入项目"继续。

**Q: 选了组件又填了自定义布局，哪个生效？**
A: 自定义布局优先。如果 `customLayout` 非空，Claude 按自定义描述生成；为空时按组件列表生成。

**Q: 一次性生成和分章生成可以切换吗？**
A: 可以随时在基本设置中切换。但建议长报告用分章模式，便于逐章精调。

**Q: Claude 找不到我的项目怎么办？**
A: 直接告诉 Claude 项目的绝对路径（如"我的项目在 /Users/xxx/Documents/货币基金报告/"）。Claude 会直接读取该路径下的 config.json。

**Q: 可以在多台电脑上协作吗？**
A: 可以。项目文件夹独立自包含（config.json + data/ + chapter 文件），通过 git 同步或 U 盘拷贝到另一台电脑，用"导入项目"加载即可。

**Q: 生成过程中能回到工具页面修改配置吗？**
A: 可以。如果 Claude 正在处理某章，建议等该章完成后再改配置。修改后的配置会在 Claude 生成下一章时生效。

